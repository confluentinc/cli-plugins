description: Creates a flink compute pool, associates or lists cluster ids for database and creates a cluster when none is present, then starts a flink shell session
dependencies:
  - name: Python
    version: "3"
